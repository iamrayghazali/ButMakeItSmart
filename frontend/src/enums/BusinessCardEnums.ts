export enum BusinessCardBackground {
    None,
    url
}

// color price
export enum BusinessCardColor {
    Black = 2,
    Green = 5,
    White = 0,
}

// layout price
export enum BusinessCardLayout {
    Classic = 0,
    Standard = 1,
    Default = 2,
}

// base product price
export const ProductBasePrice = {
    BusinessCard: 10,
};